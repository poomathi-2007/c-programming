#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a,path;
    scanf("%d",&a);
    path=(a/10)+(a%10);
    printf("Bird said:%d\n",a);
    printf("Alice must go in path-%d",path);
    return 0;
}
